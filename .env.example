# Copy this file to `.env` (already gitignored — never committed) and fill in your key.
# Get a key at https://aistudio.google.com/apikey

GEMINI_API_KEY=your-key-here

# Optional overrides (defaults shown) — only needed if Google renames/retires a model id.
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_MODEL_SMALL=gemini-2.5-flash-lite
